
name: Build iOS IPA 🍏

on:
  push:
    branches:
      - main
  workflow_dispatch: # Manuel tetikleme için

jobs:
  build:
    name: EAS Build
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Setup Repo
        uses: actions/checkout@v3

      - name: 🏗 Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18.x
          # cache: npm <- BU SATIRI KALDIRDIK (HATA VERMEMESI ICIN)

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v2
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Install Dependencies
        run: |
          cd mobile
          # Garanti olsun diye lock dosyasını CI ortamında taze oluşturuyoruz
          npm install --package-lock-only
          npm ci || npm install

      - name: 🚀 Build iOS IPA
        # profil_imzali profilini kullanarak build alır.
        # Sertifikalarınız Expo sunucularında kayıtlı olmalıdır (ilk sefer lokalden yapmanız önerilir).
        run: cd mobile && eas build --platform ios --profile profil_imzali --non-interactive --no-wait
